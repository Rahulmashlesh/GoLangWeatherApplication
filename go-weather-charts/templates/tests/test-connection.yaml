apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "go-weather-charts.fullname" . }}-test-connection"
  labels:
    {{- include "go-weather-charts.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "go-weather-charts.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
