apiVersion: v1
kind: Service
metadata:
  name: {{ include "go-weather-charts.fullname" . }}
  labels:
    {{- include "go-weather-charts.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "go-weather-charts.selectorLabels" . | nindent 4 }}
